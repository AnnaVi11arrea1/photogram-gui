<h1>Photo Details</h1>


<dl>
  <dt>
  Image
  </dt>
  <dd>
    <img src="<%= @photo_id.image %>" height="300px">
  </dd>

  <dt>
    Caption
  </dt>
  <dd>
    <%= @photo_id.caption %>
  </dd>

  <dt>
    Owner
  </dt>
  <dd>
    <%= @photo_id.owner_id %>
  </dd>

  <dt>
    Posted
  </dt>
  <dd>
    <%= @photo_id.created_at %>
  </dd>

  <dt>
    Edit Photo
  </dt>
    <dd>
      <form action="/photos/<%= @photo_id %>/edit" method="post">Edit photo</a>
      <label for="userphoto">Image</label>
      <input id="userphoto" type="text" name="userphoto" placeholder="Enter a URL for the image..." value="<%= @photo_id.image %>">

      <label for="usercaption">Caption</label>
      <input id="usercaption" type="text" name="usercaption" placeholder="Enter a caption for the image..." value="<%= @photo_id.caption %>">

      <button type="submit">Update photo</button>
      </form>
    </dd>

    <dt>
      Delete Photo
    </dt>
    <dd>
      <a href="/delete_photo/<%= @photo_id.id %>">Delete this photo</a>
    </dd>
</dl>

<h2>Comments</h2>

<table>
  <tr>
    <th>Commnetor</th>
    <th>Comment</th>
    <th>Posted</th>
  </tr>

<% the_id = params.fetch("the_id") %>
<% matching_comments = Comment.where({:id => the_id})%>

<% matching_comments.each do |comment| %>
  <tr>
    <td><%= matching_comments.author_id%></td>
    <td><%= matching_comments.body %></td>
    <td><%= matching_comments.created_at %></td>

  </tr>

<% end %>
</table>
